
#' Calculate Commercial viability layer
#' @param roads SpatRaster of roads buffers
#' @param fetch SpatRaster of fetch
#' @param sed_dep SpatRaster of mean sediment deposition for the period
#' @param depth SpatRaster of binned water depths
#' @param verbose logical, whether messages should print. Default is TRUE, message print
#' @return SpatRaster
#' @export
get_cv <- function(
    roads,
    fetch,
    sed_dep,
    depth,
    verbose = TRUE
    ){



}
